"""
This is just a placeholder file.

Since Zendesk help articles are already human-curated structure, we do not need to run another
ingestion process to auto-detect "categories". Zendesk help articles are already categorized by the
wonder people who have wrote them.
Categories > Sections > Articles
By default, we can use these fields to filter.
-- Bryan A
"""